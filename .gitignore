# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
.terraform.lock.hcl
terraform.tfvars

# Secrets (Kubernetes Secret manifests with real credentials)
**/secrets.yaml
**/secret.yaml

# Allow ExternalSecret manifests (safe — just references, not actual secrets)
!**/external-secret.yaml
!**/external-secrets-config.yaml
!apps/external-secrets/**

# Keys and certs
*.key
*.pem

# Backups
backups/
*.sql
*.tar.gz

# OS
.DS_Store
*.swp
*.swo
*~

# IDE
.idea/
.vscode/
